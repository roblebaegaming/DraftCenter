import{NextResponse}from"next/server";import{createAdminClient}from"../../../../lib/supabase/admin";export const runtime="nodejs";export const maxDuration=60;function authorized(r){const s=process.env.CRON_SECRET;return Boolean(s)&&r.headers.get("authorization")===`Bearer ${s}`;}
export async function GET(request){if(!authorized(request))return NextResponse.json({error:"Unauthorized"},{status:401});const supabase=createAdminClient();const{data:requests,error}=await supabase.from("account_deletion_requests").select("user_id,requested_at").is("cancelled_at",null).lte("execute_after",new Date().toISOString()).limit(50);if(error)return NextResponse.json({error:error.message},{status:500});let deleted=0,blocked=0;for(const item of requests||[]){const{count}=await supabase.from("leagues").select("id",{count:"exact",head:true}).eq("created_by",item.user_id);if(count){blocked+=1;await supabase.from("account_deletion_requests").update({last_error:"Primary commissioner leagues still require transfer or deletion."}).eq("user_id",item.user_id);continue;}try{const{data:objects}=await supabase.storage.from("avatars").list(item.user_id,{limit:1000});if(objects?.length)await supabase.storage.from("avatars").remove(objects.map(object=>`${item.user_id}/${object.name}`));const{error:deleteError}=await supabase.auth.admin.deleteUser(item.user_id);if(deleteError)throw deleteError;await supabase.from("account_deletion_audit").insert({request_id:crypto.randomUUID(),requested_at:item.requested_at,result:"deleted"});deleted+=1;}catch(deletionError){await supabase.from("account_deletion_requests").update({last_error:String(deletionError.message||deletionError).slice(0,1000)}).eq("user_id",item.user_id);}}
return NextResponse.json({processed:(requests||[]).length,deleted,blocked});}
