import { FORMATS } from "../../lib/seoContent";
export const metadata = { title: "Pokémon Draft Formats and VGC Regulations", description: "Browse Pokémon Champions, Scarlet and Violet, Sword and Shield, VGC, and custom draft-league formats supported by DraftCenter.", alternates: { canonical: "/formats" } };
export default function FormatsPage() { return <main className="resources-shell"><nav className="public-page-nav"><a className="quiet-button" href="/">DraftCenter</a><a className="quiet-button" href="/guides">Guides</a><a className="quiet-button" href="/pokemon">Pokédex</a></nav><header className="resources-hero"><span className="eyebrow">FORMAT LIBRARY</span><h1>Pokémon draft formats.</h1><p>Explore the ruleset eras available when creating a DraftCenter league, from VGC 2018 through Pokémon Champions and fully custom pools.</p></header><section className="resource-grid">{FORMATS.map((format) => <a key={format.slug} href={`/formats/${format.slug}`}><strong>{format.name}</strong><p>{format.subtitle}</p><span>View format →</span></a>)}</section></main>; }
